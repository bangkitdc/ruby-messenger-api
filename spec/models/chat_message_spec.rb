require "rails_helper"

RSpec.describe ChatMessage, type: :model do
  # let(:user) { create(:user) }
  # let(:conversation) { create(:conversation) }
  # let(:participant) { create(:participant, user: user, conversation: conversation) }
  # let(:chat_message) { create(:chat_message, user: user, conversation: conversation) }

  # describe "#chat_message" do
  #   it {
  #     expect(chat_message.user_id).to eq(user.id)
  #   }
  # end
end
